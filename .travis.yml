dist: trusty
addons:
  sonarqube:
    token: SONAR_TOKEN
    github_token: SONAR_GITHUB_TOKEN
jdk:
  - oraclejdk8
script:
  - sonar-scanner -X -Dsonar.analysis.mode=preview -Dsonar.login=$SONAR_TOKEN
cache:
  directories:
  - "$HOME/.sonar/cache"
notifications:
  email: false
